<li class="ai1ec-dropdown ai1ec-author-filter {{ active_class }}">
	<a class="ai1ec-dropdown-toggle" data-toggle="ai1ec-dropdown">
		<i class="ai1ec-fa ai1ec-fa-user"></i>
		<span class="ai1ec-clear-filter ai1ec-tooltip-trigger"
			data-href="{{ clear_filter | e('html_attr') }}"
			{{ data_type | raw }}
			title="{{ 'Clear author filter' | __ }}">

			<i class="ai1ec-fa ai1ec-fa-times-circle"></i>
		</span>

		{{ 'Authors' | __ }}

		<span class="caret"></span>

	</a>

	<div class="ai1ec-dropdown-menu">
		{% for author in authors %}
			<div data-term="{{ author.user_id }}"
				{% if author.user_id in selected_auth_ids %}
					class="ai1ec-active"
				{% endif %} >

				<a class="ai1ec-load-view ai1ec-author"
					{% if author.display_name is not empty %}
						title="{{ author.display_name | e('html_attr') }}"
					{% endif %}
					{{ data_type | raw }}
					href="{{ author.href | e('html_attr') }}" >

					{{ author.display_name }}
				</a>
			</div>
		{% endfor %}
	</div>
</li>
